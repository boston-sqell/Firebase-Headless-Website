{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "wix dev",
      "name": "Development server",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
